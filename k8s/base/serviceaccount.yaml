apiVersion: v1
kind: ServiceAccount
metadata:
  name: gkorgapp1
  labels:
    app: gkorgapp1
    tenant: gkorg
  # Uncomment and update for IRSA (IAM Roles for Service Accounts)
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/gkorgapp1-role
